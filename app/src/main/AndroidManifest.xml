<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.kodec.gsa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".WisataActivity" />
        <!-- <activity android:name=".SplashActivity"></activity> -->
        <activity android:name=".Paket1Activity" />
        <activity android:name=".PayPaket1Activity" />
        <activity android:name=".SelesaiActivity" />
        <activity android:name=".BeritaActivity" />
        <activity android:name=".PromoActivity" />
        <activity android:name=".TentangActivity" />
        <activity android:name=".Berita1Activity" />
        <activity android:name=".Promo1Activity"></activity>
    </application>

</manifest>